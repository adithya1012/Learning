(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
  typeof define === 'function' && define.amd ? define(['exports'], factory) :
  (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory(global.TypesonNamespace = {}));
})(this, (function (exports) { 'use strict';

  function _arrayLikeToArray$1(r, a) {
    (null == a || a > r.length) && (a = r.length);
    for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
    return n;
  }
  function _arrayWithHoles(r) {
    if (Array.isArray(r)) return r;
  }
  function _arrayWithoutHoles(r) {
    if (Array.isArray(r)) return _arrayLikeToArray$1(r);
  }
  function asyncGeneratorStep(n, t, e, r, o, a, c) {
    try {
      var i = n[a](c),
        u = i.value;
    } catch (n) {
      return void e(n);
    }
    i.done ? t(u) : Promise.resolve(u).then(r, o);
  }
  function _asyncToGenerator(n) {
    return function () {
      var t = this,
        e = arguments;
      return new Promise(function (r, o) {
        var a = n.apply(t, e);
        function _next(n) {
          asyncGeneratorStep(a, r, o, _next, _throw, "next", n);
        }
        function _throw(n) {
          asyncGeneratorStep(a, r, o, _next, _throw, "throw", n);
        }
        _next(void 0);
      });
    };
  }
  function _classCallCheck$1(a, n) {
    if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
  }
  function _defineProperties(e, r) {
    for (var t = 0; t < r.length; t++) {
      var o = r[t];
      o.enumerable = o.enumerable || false, o.configurable = true, "value" in o && (o.writable = true), Object.defineProperty(e, _toPropertyKey$1(o.key), o);
    }
  }
  function _createClass$1(e, r, t) {
    return r && _defineProperties(e.prototype, r), Object.defineProperty(e, "prototype", {
      writable: false
    }), e;
  }
  function _defineProperty$1(e, r, t) {
    return (r = _toPropertyKey$1(r)) in e ? Object.defineProperty(e, r, {
      value: t,
      enumerable: true,
      configurable: true,
      writable: true
    }) : e[r] = t, e;
  }
  function _iterableToArray(r) {
    if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r);
  }
  function _iterableToArrayLimit(r, l) {
    var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
    if (null != t) {
      var e,
        n,
        i,
        u,
        a = [],
        f = true,
        o = false;
      try {
        if (i = (t = t.call(r)).next, 0 === l) ; else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
      } catch (r) {
        o = true, n = r;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a;
    }
  }
  function _nonIterableRest() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _nonIterableSpread() {
    throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function ownKeys$1(e, r) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r && (o = o.filter(function (r) {
        return Object.getOwnPropertyDescriptor(e, r).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread2$1(e) {
    for (var r = 1; r < arguments.length; r++) {
      var t = null != arguments[r] ? arguments[r] : {};
      r % 2 ? ownKeys$1(Object(t), true).forEach(function (r) {
        _defineProperty$1(e, r, t[r]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$1(Object(t)).forEach(function (r) {
        Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
      });
    }
    return e;
  }
  function _regenerator() {
    /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */
    var e,
      t,
      r = "function" == typeof Symbol ? Symbol : {},
      n = r.iterator || "@@iterator",
      o = r.toStringTag || "@@toStringTag";
    function i(r, n, o, i) {
      var c = n && n.prototype instanceof Generator ? n : Generator,
        u = Object.create(c.prototype);
      return _regeneratorDefine(u, "_invoke", function (r, n, o) {
        var i,
          c,
          u,
          f = 0,
          p = o || [],
          y = false,
          G = {
            p: 0,
            n: 0,
            v: e,
            a: d,
            f: d.bind(e, 4),
            d: function (t, r) {
              return i = t, c = 0, u = e, G.n = r, a;
            }
          };
        function d(r, n) {
          for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) {
            var o,
              i = p[t],
              d = G.p,
              l = i[2];
            r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0));
          }
          if (o || r > 1) return a;
          throw y = true, n;
        }
        return function (o, p, l) {
          if (f > 1) throw TypeError("Generator is already running");
          for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) {
            i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u);
            try {
              if (f = 2, i) {
                if (c || (o = "next"), t = i[o]) {
                  if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object");
                  if (!t.done) return t;
                  u = t.value, c < 2 && (c = 0);
                } else 1 === c && (t = i.return) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1);
                i = e;
              } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break;
            } catch (t) {
              i = e, c = 1, u = t;
            } finally {
              f = 1;
            }
          }
          return {
            value: t,
            done: y
          };
        };
      }(r, o, i), true), u;
    }
    var a = {};
    function Generator() {}
    function GeneratorFunction() {}
    function GeneratorFunctionPrototype() {}
    t = Object.getPrototypeOf;
    var c = [][n] ? t(t([][n]())) : (_regeneratorDefine(t = {}, n, function () {
        return this;
      }), t),
      u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c);
    function f(e) {
      return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e;
    }
    return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine(u), _regeneratorDefine(u, o, "Generator"), _regeneratorDefine(u, n, function () {
      return this;
    }), _regeneratorDefine(u, "toString", function () {
      return "[object Generator]";
    }), (_regenerator = function () {
      return {
        w: i,
        m: f
      };
    })();
  }
  function _regeneratorDefine(e, r, n, t) {
    var i = Object.defineProperty;
    try {
      i({}, "", {});
    } catch (e) {
      i = 0;
    }
    _regeneratorDefine = function (e, r, n, t) {
      function o(r, n) {
        _regeneratorDefine(e, r, function (e) {
          return this._invoke(r, n, e);
        });
      }
      r ? i ? i(e, r, {
        value: n,
        enumerable: !t,
        configurable: !t,
        writable: !t
      }) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2));
    }, _regeneratorDefine(e, r, n, t);
  }
  function _slicedToArray$1(r, e) {
    return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray$1(r, e) || _nonIterableRest();
  }
  function _toConsumableArray$1(r) {
    return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray$1(r) || _nonIterableSpread();
  }
  function _toPrimitive(t, r) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r);
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r ? String : Number)(t);
  }
  function _toPropertyKey$1(t) {
    var i = _toPrimitive(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _typeof$1(o) {
    "@babel/helpers - typeof";

    return _typeof$1 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) {
      return typeof o;
    } : function (o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, _typeof$1(o);
  }
  function _unsupportedIterableToArray$1(r, a) {
    if (r) {
      if ("string" == typeof r) return _arrayLikeToArray$1(r, a);
      var t = {}.toString.call(r).slice(8, -1);
      return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray$1(r, a) : void 0;
    }
  }

  function _arrayLikeToArray(e, t) {
    (null == t || t > e.length) && (t = e.length);
    for (var r = 0, n = Array(t); r < t; r++) n[r] = e[r];
    return n;
  }
  function _classCallCheck(e, t) {
    if (!(e instanceof t)) throw new TypeError("Cannot call a class as a function");
  }
  function _createClass(e, t, r) {
    return t && function _defineProperties(e, t) {
      for (var r = 0; r < t.length; r++) {
        var n = t[r];
        n.enumerable = n.enumerable || false, n.configurable = true, "value" in n && (n.writable = true), Object.defineProperty(e, _toPropertyKey(n.key), n);
      }
    }(e.prototype, t), Object.defineProperty(e, "prototype", {
      writable: false
    }), e;
  }
  function _defineProperty(e, t, r) {
    return (t = _toPropertyKey(t)) in e ? Object.defineProperty(e, t, {
      value: r,
      enumerable: true,
      configurable: true,
      writable: true
    }) : e[t] = r, e;
  }
  function ownKeys(e, t) {
    var r = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      t && (n = n.filter(function (t) {
        return Object.getOwnPropertyDescriptor(e, t).enumerable;
      })), r.push.apply(r, n);
    }
    return r;
  }
  function _objectSpread2(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = null != arguments[t] ? arguments[t] : {};
      t % 2 ? ownKeys(Object(r), true).forEach(function (t) {
        _defineProperty(e, t, r[t]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : ownKeys(Object(r)).forEach(function (t) {
        Object.defineProperty(e, t, Object.getOwnPropertyDescriptor(r, t));
      });
    }
    return e;
  }
  function _slicedToArray(e, t) {
    return function _arrayWithHoles(e) {
      if (Array.isArray(e)) return e;
    }(e) || function _iterableToArrayLimit(e, t) {
      var r = null == e ? null : "undefined" != typeof Symbol && e[Symbol.iterator] || e["@@iterator"];
      if (null != r) {
        var n,
          o,
          i,
          a,
          c = [],
          s = true,
          u = false;
        try {
          if (i = (r = r.call(e)).next, 0 === t) {
            if (Object(r) !== r) return;
            s = !1;
          } else for (; !(s = (n = i.call(r)).done) && (c.push(n.value), c.length !== t); s = !0);
        } catch (e) {
          u = true, o = e;
        } finally {
          try {
            if (!s && null != r["return"] && (a = r["return"](), Object(a) !== a)) return;
          } finally {
            if (u) throw o;
          }
        }
        return c;
      }
    }(e, t) || _unsupportedIterableToArray(e, t) || function _nonIterableRest() {
      throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }();
  }
  function _toConsumableArray(e) {
    return function _arrayWithoutHoles(e) {
      if (Array.isArray(e)) return _arrayLikeToArray(e);
    }(e) || function _iterableToArray(e) {
      if ("undefined" != typeof Symbol && null != e[Symbol.iterator] || null != e["@@iterator"]) return Array.from(e);
    }(e) || _unsupportedIterableToArray(e) || function _nonIterableSpread() {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }();
  }
  function _toPropertyKey(e) {
    var t = function _toPrimitive(e, t) {
      if ("object" != _typeof$1(e) || !e) return e;
      var r = e[Symbol.toPrimitive];
      if (void 0 !== r) {
        var n = r.call(e, t);
        if ("object" != _typeof$1(n)) return n;
        throw new TypeError("@@toPrimitive must return a primitive value.");
      }
      return String(e);
    }(e, "string");
    return "symbol" == _typeof$1(t) ? t : t + "";
  }
  function _typeof(e) {
    return _typeof = "function" == typeof Symbol && "symbol" == _typeof$1(Symbol.iterator) ? function (e) {
      return _typeof$1(e);
    } : function (e) {
      return e && "function" == typeof Symbol && e.constructor === Symbol && e !== Symbol.prototype ? "symbol" : _typeof$1(e);
    }, _typeof(e);
  }
  function _unsupportedIterableToArray(e, t) {
    if (e) {
      if ("string" == typeof e) return _arrayLikeToArray(e, t);
      var r = {}.toString.call(e).slice(8, -1);
      return "Object" === r && e.constructor && (r = e.constructor.name), "Map" === r || "Set" === r ? Array.from(e) : "Arguments" === r || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r) ? _arrayLikeToArray(e, t) : void 0;
    }
  }
  var e = _createClass(function TypesonPromise(e) {
    _classCallCheck(this, TypesonPromise), this.p = new Promise(e);
  });
  e.__typeson__type__ = "TypesonPromise", "undefined" != typeof Symbol && Object.defineProperty(e.prototype, Symbol.toStringTag, {
    get: function get() {
      return "TypesonPromise";
    }
  }), e.prototype.then = function (t, r) {
    var n = this;
    return new e(function (e, o) {
      n.p.then(function (r) {
        e(t ? t(r) : r);
      })["catch"](function (e) {
        return r ? r(e) : Promise.reject(e);
      }).then(e, o);
    });
  }, e.prototype["catch"] = function (e) {
    return this.then(function () {}, e);
  }, e.resolve = function (t) {
    return new e(function (e) {
      e(t);
    });
  }, e.reject = function (t) {
    return new e(function (e, r) {
      r(t);
    });
  }, e.all = function (t) {
    return new e(function (e, r) {
      Promise.all(t.map(function (e) {
        return null != e && e.constructor && "__typeson__type__" in e.constructor && "TypesonPromise" === e.constructor.__typeson__type__ ? e.p : e;
      })).then(e, r);
    });
  }, e.race = function (t) {
    return new e(function (e, r) {
      Promise.race(t.map(function (e) {
        return null != e && e.constructor && "__typeson__type__" in e.constructor && "TypesonPromise" === e.constructor.__typeson__type__ ? e.p : e;
      })).then(e, r);
    });
  }, e.allSettled = function (t) {
    return new e(function (e, r) {
      Promise.allSettled(t.map(function (e) {
        return null != e && e.constructor && "__typeson__type__" in e.constructor && "TypesonPromise" === e.constructor.__typeson__type__ ? e.p : e;
      })).then(e, r);
    });
  };
  var t = Object.hasOwn,
    r = Object.getPrototypeOf;
  function isThenable(e, t) {
    return isObject(e) && "function" == typeof e.then && (!t || "function" == typeof e["catch"]);
  }
  function toStringTag(e) {
    return Object.prototype.toString.call(e).slice(8, -1);
  }
  function hasConstructorOf(e, n) {
    if (!e || "object" !== _typeof(e)) return false;
    var o = r(e);
    if (!o) return null === n;
    var i = t(o, "constructor") && o.constructor;
    return "function" != typeof i ? null === n : n === i || null !== n && Function.prototype.toString.call(i) === Function.prototype.toString.call(n) || "function" == typeof n && "string" == typeof i.__typeson__type__ && i.__typeson__type__ === n.__typeson__type__;
  }
  function isPlainObject(e) {
    return !(!e || "Object" !== toStringTag(e)) && (!r(e) || hasConstructorOf(e, Object));
  }
  function isUserObject(e) {
    if (!e || "Object" !== toStringTag(e)) return false;
    var t = r(e);
    return !t || hasConstructorOf(e, Object) || isUserObject(t);
  }
  function isObject(e) {
    return null !== e && "object" === _typeof(e);
  }
  function escapeKeyPathComponent(e) {
    return e.replaceAll("''", "''''").replace(/^$/, "''").replaceAll("~", "~0").replaceAll(".", "~1");
  }
  function unescapeKeyPathComponent(e) {
    return e.replaceAll("~1", ".").replaceAll("~0", "~").replace(/^''$/, "").replaceAll("''''", "''");
  }
  function getByKeyPath(e, t) {
    if ("" === t) return e;
    if (null === e || "object" !== _typeof(e)) throw new TypeError("Unexpected non-object type");
    var r = t.indexOf(".");
    if (-1 !== r) {
      var n = e[unescapeKeyPathComponent(t.slice(0, r))];
      return void 0 === n ? void 0 : getByKeyPath(n, t.slice(r + 1));
    }
    return e[unescapeKeyPathComponent(t)];
  }
  function setAtKeyPath(e, t, r) {
    if ("" === t) return r;
    if (!e || "object" !== _typeof(e)) throw new TypeError("Unexpected non-object type");
    if ("__proto__" === t) throw new TypeError("Invalid property");
    var n = t.indexOf(".");
    return -1 !== n ? setAtKeyPath(e[unescapeKeyPathComponent(t.slice(0, n))], t.slice(n + 1), r) : (e[unescapeKeyPathComponent(t)] = r, e);
  }
  function getJSONType(e) {
    return null === e ? "null" : Array.isArray(e) ? "array" : _typeof(e);
  }
  function _await(e, t, r) {
    return e && e.then || (e = Promise.resolve(e)), t ? e.then(t) : e;
  }
  var n = Object.keys,
    o = Object.hasOwn,
    i$1 = Array.isArray,
    a = ["type", "replaced", "iterateIn", "iterateUnsetNumeric", "addLength"];
  function _async(e) {
    return function () {
      for (var t = [], r = 0; r < arguments.length; r++) t[r] = arguments[r];
      try {
        return Promise.resolve(e.apply(this, t));
      } catch (e) {
        return Promise.reject(e);
      }
    };
  }
  function nestedPathsFirst(e, t) {
    var r, n;
    if ("" === e.keypath) return -1;
    var o = null !== (r = e.keypath.match(/\./g)) && void 0 !== r ? r : 0,
      i = null !== (n = t.keypath.match(/\./g)) && void 0 !== n ? n : 0;
    return o && (o = o.length), i && (i = i.length), o > i ? -1 : o < i ? 1 : e.keypath < t.keypath ? -1 : e.keypath > t.keypath ? 1 : 0;
  }
  var c = function () {
      return _createClass(function Typeson(e) {
        _classCallCheck(this, Typeson), this.options = e, this.plainObjectReplacers = [], this.nonplainObjectReplacers = [], this.revivers = {}, this.types = {};
      }, [{
        key: "stringify",
        value: function stringify(e, t, r, n) {
          n = _objectSpread2(_objectSpread2(_objectSpread2({}, this.options), n), {}, {
            stringification: true
          });
          var o = this.encapsulate(e, null, n);
          return i$1(o) ? JSON.stringify(o[0], t, r) : o.then(function (e) {
            return JSON.stringify(e, t, r);
          });
        }
      }, {
        key: "stringifySync",
        value: function stringifySync(e, t, r, n) {
          return this.stringify(e, t, r, _objectSpread2(_objectSpread2({
            throwOnBadSyncType: true
          }, n), {}, {
            sync: true
          }));
        }
      }, {
        key: "stringifyAsync",
        value: function stringifyAsync(e, t, r, n) {
          return this.stringify(e, t, r, _objectSpread2(_objectSpread2({
            throwOnBadSyncType: true
          }, n), {}, {
            sync: false
          }));
        }
      }, {
        key: "parse",
        value: function parse(e, t, r) {
          return r = _objectSpread2(_objectSpread2(_objectSpread2({}, this.options), r), {}, {
            parse: true
          }), this.revive(JSON.parse(e, t), r);
        }
      }, {
        key: "parseSync",
        value: function parseSync(e, t, r) {
          return this.parse(e, t, _objectSpread2(_objectSpread2({
            throwOnBadSyncType: true
          }, r), {}, {
            sync: true
          }));
        }
      }, {
        key: "parseAsync",
        value: function parseAsync(e, t, r) {
          return this.parse(e, t, _objectSpread2(_objectSpread2({
            throwOnBadSyncType: true
          }, r), {}, {
            sync: false
          }));
        }
      }, {
        key: "specialTypeNames",
        value: function specialTypeNames(e, t) {
          var r = arguments.length > 2 && void 0 !== arguments[2] ? arguments[2] : {};
          return r.returnTypeNames = true, this.encapsulate(e, t, r);
        }
      }, {
        key: "rootTypeName",
        value: function rootTypeName(e, t) {
          var r = arguments.length > 2 && void 0 !== arguments[2] ? arguments[2] : {};
          return r.iterateNone = true, this.encapsulate(e, t, r);
        }
      }, {
        key: "encapsulate",
        value: function encapsulate(t, r, c) {
          var s = this,
            u = _objectSpread2(_objectSpread2({
              sync: true
            }, this.options), c),
            y = u.sync,
            l = {},
            p = [],
            f = [],
            v = [],
            h = !("cyclic" in u) || u.cyclic,
            d = u.encapsulateObserver,
            b = function finish(e) {
              var t = Object.values(l);
              if (u.iterateNone) return t.length ? t[0] : getJSONType(e);
              if (t.length) {
                if (u.returnTypeNames) return _toConsumableArray(new Set(t));
                e && isPlainObject(e) && !o(e, "$types") ? e.$types = l : e = {
                  $: e,
                  $types: {
                    $: l
                  }
                };
              } else isObject(e) && o(e, "$types") && (e = {
                $: e,
                $types: true
              });
              return !u.returnTypeNames && e;
            },
            _ = _async(function (t, r) {
              return _await(Promise.all(r.map(function (e) {
                return e[1].p;
              })), function (n) {
                return _await(Promise.all(n.map(_async(function (n) {
                  var o = false,
                    i = [],
                    a = _slicedToArray(r.splice(0, 1), 1),
                    c = _slicedToArray(a[0], 7),
                    s = c[0],
                    u = c[2],
                    y = c[3],
                    l = c[4],
                    p = c[5],
                    f = c[6],
                    v = m(s, n, u, y, i, true, f),
                    h = hasConstructorOf(v, e);
                  return function _invoke(e, t) {
                    var r = e();
                    return r && r.then ? r.then(t) : t(r);
                  }(function () {
                    if (s && h) return _await(v.p, function (e) {
                      l[p] = e;
                      var r = _(t, i);
                      return o = true, r;
                    });
                  }, function (e) {
                    return o ? e : (s ? l[p] = v : t = h ? v.p : v, _(t, i));
                  });
                }))), function () {
                  return t;
                });
              });
            }),
            O = function _adaptBuiltinStateObjectProperties(e, t, r) {
              Object.assign(e, t);
              var n = a.map(function (t) {
                var r = e[t];
                return delete e[t], r;
              });
              r(), a.forEach(function (t, r) {
                e[t] = n[r];
              });
            },
            m = function _encapsulate(t, r, a, c, y, v, h) {
              var b,
                _ = {},
                g = _typeof(r),
                S = d ? function (n) {
                  var o,
                    i = null !== (o = null != h ? h : c.type) && void 0 !== o ? o : getJSONType(r);
                  d(Object.assign(null != n ? n : _, {
                    keypath: t,
                    value: r,
                    cyclic: a,
                    stateObj: c,
                    promisesData: y,
                    resolvingTypesonPromise: v,
                    awaitingTypesonPromise: hasConstructorOf(r, e)
                  }, {
                    type: i
                  }));
                } : null;
              if (["string", "boolean", "number", "undefined"].includes(g)) return void 0 === r || Number.isNaN(r) || r === Number.NEGATIVE_INFINITY || r === Number.POSITIVE_INFINITY || 0 === r ? (b = c.replaced ? r : j(t, r, c, y, false, v, S)) !== r && (_ = {
                replaced: b
              }) : b = r, S && S(), b;
              if (null === r) return S && S(), r;
              if (a && !c.iterateIn && !c.iterateUnsetNumeric && r && "object" === _typeof(r)) {
                var w = p.indexOf(r);
                if (-1 !== w) return l[t] = "#", S && S({
                  cyclicKeypath: f[w]
                }), "#" + f[w];
                true === a && (p.push(r), f.push(t));
              }
              var T,
                P = isPlainObject(r),
                A = i$1(r),
                C = (P || A) && (!s.plainObjectReplacers.length || c.replaced) || c.iterateIn ? r : j(t, r, c, y, P || A, null, S);
              if (C !== r ? (b = C, _ = {
                replaced: C
              }) : "" === t && hasConstructorOf(r, e) ? (y.push([t, r, a, c, void 0, void 0, c.type]), b = r) : A && "object" !== c.iterateIn || "array" === c.iterateIn ? (T = new Array(r.length), _ = {
                clone: T
              }) : (["function", "symbol"].includes(_typeof(r)) || "toJSON" in r || hasConstructorOf(r, e) || hasConstructorOf(r, Promise) || hasConstructorOf(r, ArrayBuffer)) && !P && "object" !== c.iterateIn ? b = r : (T = {}, c.addLength && (T.length = r.length), _ = {
                clone: T
              }), S && S(), u.iterateNone) return null != T ? T : b;
              if (!T) return b;
              if (c.iterateIn) {
                var k = function _loop(n) {
                  var i = {
                    ownKeys: o(r, n)
                  };
                  O(c, i, function () {
                    var o = t + (t ? "." : "") + escapeKeyPathComponent(n),
                      i = m(o, r[n], Boolean(a), c, y, v);
                    hasConstructorOf(i, e) ? y.push([o, i, Boolean(a), c, T, n, c.type]) : void 0 !== i && (T[n] = i);
                  });
                };
                for (var N in r) k(N);
                S && S({
                  endIterateIn: true,
                  end: true
                });
              } else n(r).forEach(function (n) {
                var o = t + (t ? "." : "") + escapeKeyPathComponent(n);
                O(c, {
                  ownKeys: true
                }, function () {
                  var t = m(o, r[n], Boolean(a), c, y, v);
                  hasConstructorOf(t, e) ? y.push([o, t, Boolean(a), c, T, n, c.type]) : void 0 !== t && (T[n] = t);
                });
              }), S && S({
                endIterateOwn: true,
                end: true
              });
              if (c.iterateUnsetNumeric) {
                for (var I = r.length, E = function _loop2(n) {
                    if (!(n in r)) {
                      var o = "".concat(t).concat(t ? "." : "").concat(String(n));
                      O(c, {
                        ownKeys: false
                      }, function () {
                        var t = m(o, void 0, Boolean(a), c, y, v);
                        hasConstructorOf(t, e) ? y.push([o, t, Boolean(a), c, T, n, c.type]) : void 0 !== t && (T[n] = t);
                      });
                    }
                  }, K = 0; K < I; K++) E(K);
                S && S({
                  endIterateUnsetNumeric: true,
                  end: true
                });
              }
              return T;
            },
            j = function replace(e, t, r, n, o, i, a) {
              for (var c = o ? s.plainObjectReplacers : s.nonplainObjectReplacers, u = c.length; u--;) {
                var p = c[u];
                if (p.test(t, r)) {
                  var f = p.type;
                  if (s.revivers[f]) {
                    var v = l[e];
                    l[e] = v ? [f].concat(v) : f;
                  }
                  if (Object.assign(r, {
                    type: f,
                    replaced: true
                  }), (y || !p.replaceAsync) && !p.replace) return a && a({
                    typeDetected: true
                  }), m(e, t, h && "readonly", r, n, i, f);
                  a && a({
                    replacing: true
                  });
                  var d = void 0;
                  if (y || !p.replaceAsync) {
                    if (void 0 === p.replace) throw new TypeError("Missing replacer");
                    d = p.replace(t, r);
                  } else d = p.replaceAsync(t, r);
                  return m(e, d, h && "readonly", r, n, i, f);
                }
              }
              return t;
            },
            g = m("", t, h, null != r ? r : {}, v);
          if (v.length) return y && u.throwOnBadSyncType ? function () {
            throw new TypeError("Sync method requested but async result obtained");
          }() : Promise.resolve(_(g, v)).then(b);
          if (!y && u.throwOnBadSyncType) throw new TypeError("Async method requested but sync result obtained");
          return u.stringification && y ? [b(g)] : y ? b(g) : Promise.resolve(b(g));
        }
      }, {
        key: "encapsulateSync",
        value: function encapsulateSync(e, t, r) {
          return this.encapsulate(e, t, _objectSpread2(_objectSpread2({
            throwOnBadSyncType: true
          }, r), {}, {
            sync: true
          }));
        }
      }, {
        key: "encapsulateAsync",
        value: function encapsulateAsync(e, t, r) {
          return this.encapsulate(e, t, _objectSpread2(_objectSpread2({
            throwOnBadSyncType: true
          }, r), {}, {
            sync: false
          }));
        }
      }, {
        key: "revive",
        value: function revive(t, r) {
          var o = this,
            a = _objectSpread2(_objectSpread2({
              sync: true
            }, this.options), r),
            c = a.sync;
          function finishRevival(e) {
            if (c) return e;
            if (a.throwOnBadSyncType) throw new TypeError("Async method requested but sync result obtained");
            return Promise.resolve(e);
          }
          if (!t || "object" !== _typeof(t) || Array.isArray(t)) return finishRevival(t);
          var u = t.$types;
          if (true === u) return finishRevival(t.$);
          if (!u || "object" !== _typeof(u) || Array.isArray(u)) return finishRevival(t);
          var y = [],
            l = {},
            p = true;
          u.$ && isPlainObject(u.$) && (t = t.$, u = u.$, p = false);
          var f = function executeReviver(e, t) {
              var r,
                n = _slicedToArray(null !== (r = o.revivers[e]) && void 0 !== r ? r : [], 1)[0];
              if (!n) throw new Error("Unregistered type: " + e);
              if (c && !("revive" in n)) return t;
              if (!c && n.reviveAsync) return n.reviveAsync(t, l);
              if (n.revive) return n.revive(t, l);
              throw new Error("Missing reviver");
            },
            v = [];
          function checkUndefined(e) {
            return hasConstructorOf(e, s) ? void 0 : e;
          }
          var h,
            d = function revivePlainObjects() {
              var r = [];
              if (!u) throw new Error("Found bad `types`");
              if (Object.entries(u).forEach(function (e) {
                var t = _slicedToArray(e, 2),
                  n = t[0],
                  i = t[1];
                "#" !== i && [].concat(i).forEach(function (e) {
                  var t;
                  _slicedToArray(null !== (t = o.revivers[e]) && void 0 !== t ? t : [null, {}], 2)[1].plain && (r.push({
                    keypath: n,
                    type: e
                  }), delete u[n]);
                });
              }), r.length) return r.sort(nestedPathsFirst), r.reduce(function reducer(r, n) {
                var o = n.keypath,
                  i = n.type;
                if (isThenable(r)) return r.then(function (e) {
                  return reducer(e, {
                    keypath: o,
                    type: i
                  });
                });
                var a = getByKeyPath(t, o);
                if (hasConstructorOf(a = f(i, a), e)) return a.then(function (e) {
                  var r = setAtKeyPath(t, o, e);
                  r === e && (t = r);
                });
                var c = setAtKeyPath(t, o, a);
                c === a && (t = c);
              }, void 0);
            }();
          return hasConstructorOf(d, e) ? h = d.then(function () {
            return t;
          }) : (h = function _revive(t, r, o, a, c) {
            if (!p || "$types" !== t) {
              var l = u[t],
                h = i$1(r);
              if (h || isPlainObject(r)) {
                var d = h ? new Array(r.length) : {};
                for (n(r).forEach(function (n) {
                  var i = _revive(t + (t ? "." : "") + escapeKeyPathComponent(n), r[n], null != o ? o : d, d, n),
                    a = function set(e) {
                      return hasConstructorOf(e, s) ? d[n] = void 0 : void 0 !== e && (d[n] = e), e;
                    };
                  hasConstructorOf(i, e) ? v.push(i.then(function (e) {
                    return a(e);
                  })) : a(i);
                }), r = d; y.length;) {
                  var b = _slicedToArray(y[0], 4),
                    _ = b[0],
                    O = b[1],
                    m = b[2],
                    j = b[3],
                    g = getByKeyPath(_, O);
                  if (void 0 === g) break;
                  m[j] = g, y.splice(0, 1);
                }
              }
              if (!l) return r;
              if ("#" === l) {
                var S = getByKeyPath(o, r.slice(1));
                return void 0 === S && y.push([o, r.slice(1), a, c]), S;
              }
              return [].concat(l).reduce(function reducer(t, r) {
                if (hasConstructorOf(t, e)) return t.then(function (e) {
                  return reducer(e, r);
                });
                if ("string" != typeof r) throw new TypeError("Bad type JSON");
                return f(r, t);
              }, r);
            }
          }("", t, null), v.length && (h = e.resolve(h).then(function (t) {
            return e.all([t].concat(v));
          }).then(function (e) {
            return _slicedToArray(e, 1)[0];
          }))), isThenable(h) ? c && a.throwOnBadSyncType ? function () {
            throw new TypeError("Sync method requested but async result obtained");
          }() : hasConstructorOf(h, e) ? h.p.then(checkUndefined) : h : !c && a.throwOnBadSyncType ? function () {
            throw new TypeError("Async method requested but sync result obtained");
          }() : c ? checkUndefined(h) : Promise.resolve(checkUndefined(h));
        }
      }, {
        key: "reviveSync",
        value: function reviveSync(e, t) {
          return this.revive(e, _objectSpread2(_objectSpread2({
            throwOnBadSyncType: true
          }, t), {}, {
            sync: true
          }));
        }
      }, {
        key: "reviveAsync",
        value: function reviveAsync(e, t) {
          return this.revive(e, _objectSpread2(_objectSpread2({
            throwOnBadSyncType: true
          }, t), {}, {
            sync: false
          }));
        }
      }, {
        key: "register",
        value: function register(e, t) {
          var r = this,
            o = null != t ? t : {},
            a = function reg(e) {
              i$1(e) ? e.forEach(function (e) {
                a(e);
              }) : n(e).forEach(function (t) {
                var n;
                if ("#" === t) throw new TypeError("# cannot be used as a type name as it is reserved for cyclic objects");
                if (u.includes(t)) throw new TypeError("Plain JSON object types are reserved as type names");
                var a = e[t],
                  c = a && "function" != typeof a && !Array.isArray(a) && a.testPlainObjects ? r.plainObjectReplacers : r.nonplainObjectReplacers,
                  s = c.filter(function (e) {
                    return e.type === t;
                  });
                if (s.length && (c.splice(c.indexOf(s[0]), 1), delete r.revivers[t], delete r.types[t]), "function" == typeof a) {
                  var y = a;
                  a = {
                    test: function test(e) {
                      return e && e.constructor === y;
                    },
                    replace: function replace(e) {
                      return _objectSpread2({}, e);
                    },
                    revive: function revive(e) {
                      return Object.assign(Object.create(y.prototype), e);
                    }
                  };
                } else if (i$1(a)) {
                  var l = _slicedToArray(a, 3);
                  a = {
                    test: l[0],
                    replace: l[1],
                    revive: l[2]
                  };
                }
                if (null !== (n = a) && void 0 !== n && n.test) {
                  var p = {
                    type: t,
                    test: a.test.bind(a)
                  };
                  a.replace && (p.replace = a.replace.bind(a)), a.replaceAsync && (p.replaceAsync = a.replaceAsync.bind(a));
                  var f = "number" == typeof o.fallback ? o.fallback : o.fallback ? 0 : Number.POSITIVE_INFINITY;
                  if (a.testPlainObjects ? r.plainObjectReplacers.splice(f, 0, p) : r.nonplainObjectReplacers.splice(f, 0, p), a.revive || a.reviveAsync) {
                    var v = {};
                    a.revive && (v.revive = a.revive.bind(a)), a.reviveAsync && (v.reviveAsync = a.reviveAsync.bind(a)), r.revivers[t] = [v, {
                      plain: a.testPlainObjects
                    }];
                  }
                  r.types[t] = a;
                }
              });
            };
          return [].concat(e).forEach(function (e) {
            a(e);
          }), this;
        }
      }]);
    }(),
    s = _createClass(function Undefined() {
      _classCallCheck(this, Undefined);
    });
  s.__typeson__type__ = "TypesonUndefined";
  var u = ["null", "boolean", "number", "string", "array", "object"];

  /*
   * base64-arraybuffer
   * https://github.com/niklasvh/base64-arraybuffer
   *
   * Copyright (c) 2017-2023 Brett Zamir, 2012 Niklas von Hertzen
   * Licensed under the MIT license.
   */

  /**
   * @typedef {number} Integer
   */

  var chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';

  // Use a lookup table to find the index.
  var lookup = new Uint8Array(256);
  for (var i = 0; i < chars.length; i++) {
    lookup[/** @type {number} */chars.codePointAt(i)] = i;
  }

  /**
   * @param {ArrayBuffer} arraybuffer
   * @param {Integer} [byteOffset]
   * @param {Integer} [lngth]
   * @returns {string}
   */
  var encode = function encode(arraybuffer, byteOffset, lngth) {
    if (lngth === null || lngth === undefined) {
      lngth = arraybuffer.byteLength; // Needed for Safari
    }
    var bytes = new Uint8Array(arraybuffer, 0,
    // Default needed for Safari
    lngth);
    var len = bytes.length;
    var base64 = '';
    for (var _i = 0; _i < len; _i += 3) {
      base64 += chars[bytes[_i] >> 2];
      base64 += chars[(bytes[_i] & 3) << 4 | bytes[_i + 1] >> 4];
      base64 += chars[(bytes[_i + 1] & 15) << 2 | bytes[_i + 2] >> 6];
      base64 += chars[bytes[_i + 2] & 63];
    }
    if (len % 3 === 2) {
      base64 = base64.slice(0, -1) + '=';
    } else if (len % 3 === 1) {
      base64 = base64.slice(0, -2) + '==';
    }
    return base64;
  };

  /**
   * @param {string} base64
   * @param {{
   *   maxByteLength: number
   * }} [options]
   * @returns {ArrayBuffer}
   */
  var decode = function decode(base64, options) {
    var len = base64.length;
    if (len % 4) {
      throw new Error('Bad base64 length: not divisible by four');
    }
    var bufferLength = base64.length * 0.75;
    var p = 0;
    var encoded1, encoded2, encoded3, encoded4;
    if (base64[base64.length - 1] === '=') {
      bufferLength--;
      if (base64[base64.length - 2] === '=') {
        bufferLength--;
      }
    }

    // @ts-expect-error Second argument is not yet standard
    var arraybuffer = new ArrayBuffer(bufferLength, options),
      bytes = new Uint8Array(arraybuffer);
    for (var _i2 = 0; _i2 < len; _i2 += 4) {
      // We know the result will not be undefined, as we have a text
      //   length divisible by four
      encoded1 = lookup[/** @type {number} */base64.codePointAt(_i2)];
      encoded2 = lookup[/** @type {number} */base64.codePointAt(_i2 + 1)];
      encoded3 = lookup[/** @type {number} */base64.codePointAt(_i2 + 2)];
      encoded4 = lookup[/** @type {number} */base64.codePointAt(_i2 + 3)];
      bytes[p++] = encoded1 << 2 | encoded2 >> 4;
      bytes[p++] = (encoded2 & 15) << 4 | encoded3 >> 2;
      bytes[p++] = (encoded3 & 3) << 6 | encoded4 & 63;
    }
    return arraybuffer;
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var arraybuffer = {
    arraybuffer: {
      test: function test(x) {
        return toStringTag(x) === 'ArrayBuffer';
      },
      replace: function replace(b,
      /**
       * @type {import('typeson').StateObject &
       *   {buffers?: ArrayBuffer[]}}
       */
      stateObj) {
        if (!stateObj.buffers) {
          stateObj.buffers = [];
        }
        var index = stateObj.buffers.indexOf(b);
        if (index !== -1) {
          return {
            index: index
          };
        }
        stateObj.buffers.push(b);
        return {
          s: encode(b),
          maxByteLength: b.maxByteLength,
          resizable: b.resizable
        };
      },
      revive: function revive(b64,
      /**
       * @type {import('typeson').StateObject &
       *   {buffers?: ArrayBuffer[]}}
       */
      stateObj) {
        if (!stateObj.buffers) {
          stateObj.buffers = [];
        }
        if (Object.hasOwn(b64, 'index')) {
          return stateObj.buffers[
          /**
           * @type {{index: import('typeson').Integer}}
           */
          b64.index];
        }
        var buffer = decode(/** @type {string} */b64.s, b64.resizable ? {
          maxByteLength: b64.maxByteLength
        } : undefined);
        stateObj.buffers.push(buffer);
        return buffer;
      }
    }
  };

  // See also typed-arrays!

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var bigintObject = {
    bigintObject: {
      test: function test(x) {
        return _typeof$1(x) === 'object' && hasConstructorOf(x, BigInt);
      },
      replace: String,
      revive: function revive(s) {
        return new Object(BigInt(/** @type {string} */s));
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var bigint = {
    bigint: {
      test: function test(x) {
        return typeof x === 'bigint';
      },
      replace: String,
      // eslint-disable-next-line @stylistic/max-len -- Long
      // eslint-disable-next-line unicorn/prefer-native-coercion-functions -- Clearer
      revive: function revive(s) {
        return BigInt(/** @type {string} */s);
      }
    }
  };

  /**
   * Not currently in use internally, but provided for parity.
   * @param {ArrayBuffer} buf
   * @returns {string}
   */
  function arraybuffer2string(buf) {
    return new Uint8Array(buf).reduce(function (s, _byte) {
      return s + String.fromCodePoint(_byte);
    }, '');
  }

  /**
   *
   * @param {string} str
   * @returns {ArrayBuffer}
   */
  function string2arraybuffer(str) {
    /*
    // UTF-8 approaches
    const utf8 = unescape(encodeURIComponent(str));
    const arr = new Uint8Array(utf8.length);
    for (let i = 0; i < utf8.length; i++) {
        arr[i] = utf8.charCodeAt(i);
    }
    return arr.buffer;
     const utf8 = [];
    for (let i = 0; i < str.length; i++) {
        let charcode = str.charCodeAt(i);
        if (charcode < 0x80) utf8.push(charcode);
        else if (charcode < 0x800) {
            utf8.push(0xc0 | (charcode >> 6),
                0x80 | (charcode & 0x3f));
        } else if (charcode < 0xd800 || charcode >= 0xe000) {
            utf8.push(0xe0 | (charcode >> 12),
                0x80 | ((charcode >> 6) & 0x3f),
                0x80 | (charcode & 0x3f));
        // surrogate pair
        } else {
            i++;
            // UTF-16 encodes 0x10000-0x10FFFF by
            // subtracting 0x10000 and splitting the
            // 20 bits of 0x0-0xFFFFF into two halves
            charcode = 0x10000 + (((charcode & 0x3ff) << 10) |
                (str.charCodeAt(i) & 0x3ff));
            utf8.push(0xf0 | (charcode >> 18),
                0x80 | ((charcode >> 12) & 0x3f),
                0x80 | ((charcode >> 6) & 0x3f),
                0x80 | (charcode & 0x3f));
        }
    }
    return utf8;
    */
    /*
    // Working UTF-16 options (equivalents)
    const buf = new ArrayBuffer(str.length * 2); // 2 bytes for each char
    const bufView = new Uint16Array(buf);
    for (let i = 0, strLen = str.length; i < strLen; i++) {
        bufView[i] = str.charCodeAt(i);
    }
    return buf;
    */

    var array = new Uint8Array(str.length);
    for (var i = 0; i < str.length; i++) {
      // eslint-disable-next-line @stylistic/max-len -- Long
      // eslint-disable-next-line unicorn/prefer-code-point -- Iterating char. codes
      array[i] = str.charCodeAt(i); // & 0xff;
    }
    return array.buffer;
  }

  /* globals XMLHttpRequest, FileReader -- Polyfills */

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var blob = {
    blob: {
      test: function test(x) {
        return toStringTag(x) === 'Blob';
      },
      replace: function replace(b) {
        // Sync
        var req = new XMLHttpRequest();
        req.overrideMimeType('text/plain; charset=x-user-defined');
        req.open('GET', URL.createObjectURL(b), false); // Sync
        req.send();

        // Seems not feasible to accurately simulate
        /* c8 ignore next 3 */
        if (req.status !== 200 && req.status !== 0) {
          throw new Error('Bad Blob access: ' + req.status);
        }
        return {
          type: b.type,
          stringContents: req.responseText
        };
      },
      revive: function revive(obj) {
        var type = (/** @type {{type: string, stringContents: string}} */obj).type,
          stringContents = (/** @type {{type: string, stringContents: string}} */obj).stringContents;
        return new Blob([string2arraybuffer(stringContents)], {
          type: type
        });
      },
      replaceAsync: function replaceAsync(b) {
        return new e(function (resolve, reject) {
          /*
          if (b.isClosed) { // On MDN, but not in https://w3c.github.io/FileAPI/#dfn-Blob
              reject(new Error('The Blob is closed'));
              return;
          }
          */
          var reader = new FileReader();
          reader.addEventListener('load', function () {
            resolve({
              type: b.type,
              stringContents: arraybuffer2string(/** @type {ArrayBuffer} */reader.result)
            });
          });
          // Seems not feasible to accurately simulate
          /* c8 ignore next 3 */
          reader.addEventListener('error', function () {
            reject(reader.error);
          });
          // eslint-disable-next-line @stylistic/max-len -- Long
          // eslint-disable-next-line unicorn/prefer-blob-reading-methods -- Too new?
          reader.readAsArrayBuffer(b);
        });
      }
    }
  };

  /**
   * @todo We could use `import generateUUID from 'uuid/v4';` (but it needs
   *   crypto library, etc.; `rollup-plugin-node-builtins` doesn't recommend
   *   using its own version and though there is <https://www.npmjs.com/package/crypto-browserify>,
   *   it may be troublesome to bundle and not strongly needed)
   * @returns {string}
   */
  function generateUUID() {
    //  Adapted from original: public domain/MIT: http://stackoverflow.com/a/8809472/271577
    /* c8 ignore next */
    var d = Date.now();
    return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replaceAll(/[xy]/g, function (c) {
      /* eslint-disable no-bitwise, sonarjs/pseudo-random -- Convenient */
      var r = Math.trunc((d + Math.random() * 16) % 16);
      d = Math.floor(d / 16);
      return (c === 'x' ? r : r & 0x3 | 0x8).toString(16);
      /* eslint-enable no-bitwise, sonarjs/pseudo-random -- Convenient */
    });
  }

  /**
   * @type {{[key: (symbol|string)]: any}}
   */
  var cloneableObjectsByUUID = {};

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var cloneable = {
    cloneable: {
      test: function test(x) {
        return x && _typeof$1(x) === 'object' && typeof x[Symbol["for"]('cloneEncapsulate')] === 'function';
      },
      replace: function replace(clonable) {
        var encapsulated = clonable[Symbol["for"]('cloneEncapsulate')]();
        var uuid = generateUUID();
        cloneableObjectsByUUID[uuid] = clonable;
        return {
          uuid: uuid,
          encapsulated: encapsulated
        };
      },
      revive: function revive(obj) {
        var uuid = (/** @type {{uuid: string, encapsulated: any}} */obj).uuid,
          encapsulated = (/** @type {{uuid: string, encapsulated: any}} */obj).encapsulated;
        return cloneableObjectsByUUID[uuid][Symbol["for"]('cloneRevive')](encapsulated);
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var cryptokey = {
    cryptokey: {
      test: function test(x) {
        return toStringTag(x) === 'CryptoKey' && x.extractable;
      },
      replaceAsync: function replaceAsync(/** @type {CryptoKey} */
      key) {
        return new e(/*#__PURE__*/function () {
          var _ref = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(resolve, reject) {
            var jwk, _t;
            return _regenerator().w(function (_context) {
              while (1) switch (_context.p = _context.n) {
                case 0:
                  _context.p = 0;
                  _context.n = 1;
                  return crypto.subtle.exportKey('jwk', key);
                case 1:
                  jwk = _context.v;
                  _context.n = 3;
                  break;
                case 2:
                  _context.p = 2;
                  _t = _context.v;
                  reject(_t);
                  return _context.a(2);
                case 3:
                  resolve({
                    jwk: jwk,
                    algorithm: key.algorithm,
                    usages: key.usages
                  });
                case 4:
                  return _context.a(2);
              }
            }, _callee, null, [[0, 2]]);
          }));
          return function (_x, _x2) {
            return _ref.apply(this, arguments);
          };
        }());
      },
      revive: function revive(obj) {
        var jwk = (
          /**
          * @type {{
          *   jwk: JsonWebKey,
          *   algorithm: KeyAlgorithm,
          *   usages: KeyUsage[]
          * }}
          */
          obj).jwk,
          algorithm = (
          /**
          * @type {{
          *   jwk: JsonWebKey,
          *   algorithm: KeyAlgorithm,
          *   usages: KeyUsage[]
          * }}
          */
          obj).algorithm,
          usages = (
          /**
          * @type {{
          *   jwk: JsonWebKey,
          *   algorithm: KeyAlgorithm,
          *   usages: KeyUsage[]
          * }}
          */
          obj).usages;
        return crypto.subtle.importKey('jwk', jwk, algorithm, true, usages);
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var dataview = {
    dataview: {
      test: function test(x) {
        return toStringTag(x) === 'DataView';
      },
      replace: function replace(_ref,
      /**
       * @type {import('typeson').StateObject & {
       *  buffers?: ArrayBuffer[]
       * }}
       */
      stateObj) {
        var buffer = _ref.buffer,
          byteOffset = _ref.byteOffset,
          byteLength = _ref.byteLength;
        if (!stateObj.buffers) {
          stateObj.buffers = [];
        }
        var index = stateObj.buffers.indexOf(buffer);
        if (index !== -1) {
          return {
            index: index,
            byteOffset: byteOffset,
            byteLength: byteLength
          };
        }
        stateObj.buffers.push(buffer);
        return {
          encoded: encode(buffer),
          maxByteLength: buffer.maxByteLength,
          resizable: buffer.resizable,
          byteOffset: byteOffset,
          byteLength: byteLength
        };
      },
      revive: function revive(b64Obj,
      /**
       * @type {import('typeson').StateObject & {
       *  buffers?: ArrayBuffer[]
       * }}
       */
      stateObj) {
        if (!stateObj.buffers) {
          stateObj.buffers = [];
        }
        var byteOffset = b64Obj.byteOffset,
          byteLength = b64Obj.byteLength,
          encoded = b64Obj.encoded,
          index = b64Obj.index,
          maxByteLength = b64Obj.maxByteLength,
          resizable = b64Obj.resizable;
        var buffer;
        if ('index' in b64Obj) {
          buffer = stateObj.buffers[index];
        } else {
          buffer = decode(encoded, resizable ? {
            maxByteLength: maxByteLength
          } : maxByteLength);
          stateObj.buffers.push(buffer);
        }
        return new DataView(buffer, byteOffset, byteLength);
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var date = {
    date: {
      test: function test(x) {
        return toStringTag(x) === 'Date';
      },
      replace: function replace(dt) {
        var time = dt.getTime();
        if (Number.isNaN(time)) {
          return 'NaN';
        }
        return time;
      },
      revive: function revive(time) {
        if (time === 'NaN') {
          return new Date(Number.NaN);
        }
        return new Date(time);
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var domexception = {
    domexception: {
      test: function test(x) {
        return toStringTag(x) === 'DOMException';
      },
      replace: function replace(de) {
        // `code` is based on `name` and readonly, so no
        //   need to keep here
        return {
          name: de.name,
          message: de.message
        };
      },
      revive: function revive(_ref) {
        var message = _ref.message,
          name = _ref.name;
        return new DOMException(message, name);
      }
    }
  };

  /* globals DOMMatrix, DOMMatrixReadOnly -- Polyfills */

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var dommatrix = {};

  /* c8 ignore next */
  if (typeof DOMMatrix !== 'undefined') {
    create$5(DOMMatrix);
  }
  /* c8 ignore next */
  if (typeof DOMMatrixReadOnly !== 'undefined') {
    create$5(DOMMatrixReadOnly);
  }

  /**
   * @param {typeof DOMMatrix|typeof DOMMatrixReadOnly} Ctor
   * @returns {void}
   */
  function create$5(Ctor) {
    dommatrix[Ctor.name.toLowerCase()] = {
      test: function test(x) {
        return toStringTag(x) === Ctor.name;
      },
      replace: function replace(dm) {
        if (dm.is2D) {
          return {
            a: dm.a,
            b: dm.b,
            c: dm.c,
            d: dm.d,
            e: dm.e,
            f: dm.f
          };
        }
        return {
          m11: dm.m11,
          m12: dm.m12,
          m13: dm.m13,
          m14: dm.m14,
          m21: dm.m21,
          m22: dm.m22,
          m23: dm.m23,
          m24: dm.m24,
          m31: dm.m31,
          m32: dm.m32,
          m33: dm.m33,
          m34: dm.m34,
          m41: dm.m41,
          m42: dm.m42,
          m43: dm.m43,
          m44: dm.m44
        };
      },
      revive: function revive(o) {
        if (Object.hasOwn(o, 'a')) {
          return new Ctor([o.a, o.b, o.c, o.d, o.e, o.f]);
        }
        return new Ctor([o.m11, o.m12, o.m13, o.m14, o.m21, o.m22, o.m23, o.m24, o.m31, o.m32, o.m33, o.m34, o.m41, o.m42, o.m43, o.m44]);
      }
    };
  }

  /* globals DOMPoint, DOMPointReadOnly -- Polyfills */

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var dompoint = {};

  /* c8 ignore next */
  if (typeof DOMPoint !== 'undefined') {
    create$4(DOMPoint);
  }
  /* c8 ignore next */
  if (typeof DOMPointReadOnly !== 'undefined') {
    create$4(DOMPointReadOnly);
  }

  /**
   * @param {typeof DOMPoint|typeof DOMPointReadOnly} Ctor
   * @returns {void}
   */
  function create$4(Ctor) {
    dompoint[Ctor.name.toLowerCase()] = {
      test: function test(x) {
        return toStringTag(x) === Ctor.name;
      },
      replace: function replace(dp) {
        return {
          x: dp.x,
          y: dp.y,
          z: dp.z,
          w: dp.w
        };
      },
      revive: function revive(_ref) {
        var x = _ref.x,
          y = _ref.y,
          z = _ref.z,
          w = _ref.w;
        return new Ctor(x, y, z, w);
      }
    };
  }

  /* globals DOMQuad -- Polyfills */

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var domquad = {
    domquad: {
      test: function test(x) {
        return toStringTag(x) === 'DOMQuad';
      },
      replace: function replace(dp) {
        return {
          p1: dp.p1,
          p2: dp.p2,
          p3: dp.p3,
          p4: dp.p4
        };
      },
      revive: function revive(_ref) {
        var p1 = _ref.p1,
          p2 = _ref.p2,
          p3 = _ref.p3,
          p4 = _ref.p4;
        return new DOMQuad(p1, p2, p3, p4);
      }
    }
  };

  /* globals DOMRect, DOMRectReadOnly -- Polyfills */

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var domrect = {};

  /* c8 ignore next */
  if (typeof DOMRect !== 'undefined') {
    create$3(DOMRect);
  }
  /* c8 ignore next */
  if (typeof DOMRectReadOnly !== 'undefined') {
    create$3(DOMRectReadOnly);
  }

  /**
   * @param {typeof DOMRect|typeof DOMRectReadOnly} Ctor
   * @returns {void}
   */
  function create$3(Ctor) {
    domrect[Ctor.name.toLowerCase()] = {
      test: function test(x) {
        return toStringTag(x) === Ctor.name;
      },
      replace: function replace(dr) {
        return {
          x: dr.x,
          y: dr.y,
          width: dr.width,
          height: dr.height
        };
      },
      revive: function revive(_ref) {
        var x = _ref.x,
          y = _ref.y,
          width = _ref.width,
          height = _ref.height;
        return new Ctor(x, y, width, height);
      }
    };
  }

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var error = {
    error: {
      test: function test(x) {
        return toStringTag(x) === 'Error';
      },
      replace: function replace(_ref) {
        var name = _ref.name,
          message = _ref.message,
          cause = _ref.cause,
          stack = _ref.stack,
          fileName = _ref.fileName,
          lineNumber = _ref.lineNumber,
          columnNumber = _ref.columnNumber;
        return {
          name: name,
          message: message,
          cause: cause,
          stack: stack,
          fileName: fileName,
          lineNumber: lineNumber,
          columnNumber: columnNumber
        };
      },
      revive: function revive(obj) {
        var e =
        /**
        * @type {{
        *   name: string,
        *   cause: Error,
        *   stack: string,
        *   fileName?: string,
        *   lineNumber?: import('typeson').Integer,
        *   columnNumber?: import('typeson').Integer
        * }}
        */
        new Error(obj.message);
        e.name = obj.name;
        e.cause = obj.cause;
        e.stack = obj.stack;
        e.fileName = obj.fileName;
        e.lineNumber = obj.lineNumber;
        e.columnNumber = obj.columnNumber;
        return e;
      }
    }
  };

  /* globals InternalError -- Optional */

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var errors = {};

  // JS standard
  [TypeError, RangeError, SyntaxError, ReferenceError, EvalError, URIError].forEach(function (error) {
    return create$2(error);
  });

  /* c8 ignore next 3 */
  if (typeof AggregateError !== 'undefined') {
    create$2(AggregateError);
  }

  /* c8 ignore next 5 */
  // @ts-expect-error Non-standard
  if (typeof InternalError === 'function') {
    // @ts-expect-error Non-standard
    create$2(InternalError);
  }

  /* eslint-disable jsdoc/valid-types -- https://github.com/jsdoc-type-pratt-parser/jsdoc-type-pratt-parser/issues/131 */
  /**
   * Non-standard.
   * @typedef {{
   *     new (message?: string, options?: ErrorOptions): EvalError;
   * (message?: string, options?: ErrorOptions): EvalError;
   * }} InternalErrorConstructor
   */
  /* eslint-enable jsdoc/valid-types -- https://github.com/jsdoc-type-pratt-parser/jsdoc-type-pratt-parser/issues/131 */

  /**
   * Comprises all built-in errors.
   * @param {TypeErrorConstructor|RangeErrorConstructor|
   *   SyntaxErrorConstructor|ReferenceErrorConstructor|
   *   EvalErrorConstructor|URIErrorConstructor|
   *   AggregateErrorConstructor|InternalErrorConstructor
   * } Ctor
   * @returns {void}
   */
  function create$2(Ctor) {
    errors[Ctor.name.toLowerCase()] = {
      test: function test(x) {
        return hasConstructorOf(x, Ctor);
      },
      replace: function replace(_ref) {
        var name = _ref.name,
          message = _ref.message,
          cause = _ref.cause,
          stack = _ref.stack,
          fileName = _ref.fileName,
          lineNumber = _ref.lineNumber,
          columnNumber = _ref.columnNumber,
          errs = _ref.errors;
        return {
          name: name,
          message: message,
          cause: cause,
          stack: stack,
          fileName: fileName,
          lineNumber: lineNumber,
          columnNumber: columnNumber,
          errors: errs
        };
      },
      revive: function revive(obj) {
        var isAggregateError = typeof AggregateError !== 'undefined' && Ctor === AggregateError;
        var e =
        /**
        * @type {{
        *   name: string,
        *   cause: Error,
        *   stack: string,
        *   fileName?: string,
        *   lineNumber?: import('typeson').Integer,
        *   columnNumber?: import('typeson').Integer
        * }}
        */
        isAggregateError ? new (/** @type {AggregateErrorConstructor} */
        Ctor)(obj.errors, obj.message) : new (
        /**
        * @type {TypeErrorConstructor|RangeErrorConstructor|
        *   SyntaxErrorConstructor|ReferenceErrorConstructor|
        *   EvalErrorConstructor|URIErrorConstructor|
        *   InternalErrorConstructor}
        */
        Ctor)(obj.message);
        e.name = obj.name;
        e.cause = obj.cause;
        e.stack = obj.stack;
        e.fileName = obj.fileName;
        e.lineNumber = obj.lineNumber;
        e.columnNumber = obj.columnNumber;
        return e;
      }
    };
  }

  /* globals XMLHttpRequest, FileReader -- Polyfills */

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var file = {
    file: {
      test: function test(x) {
        return toStringTag(x) === 'File';
      },
      replace: function replace(f) {
        // Sync
        var req = new XMLHttpRequest();
        req.overrideMimeType('text/plain; charset=x-user-defined');
        req.open('GET', URL.createObjectURL(f), false); // Sync
        req.send();

        // Seems not feasible to accurately simulate
        /* c8 ignore next 3 */
        if (req.status !== 200 && req.status !== 0) {
          throw new Error('Bad File access: ' + req.status);
        }
        return {
          type: f.type,
          stringContents: req.responseText,
          name: f.name,
          lastModified: f.lastModified
        };
      },
      revive: function revive(_ref) {
        var name = _ref.name,
          type = _ref.type,
          stringContents = _ref.stringContents,
          lastModified = _ref.lastModified;
        return new File([string2arraybuffer(stringContents)], name, {
          type: type,
          lastModified: lastModified
        });
      },
      replaceAsync: function replaceAsync(f) {
        return new e(function (resolve, reject) {
          /*
          if (f.isClosed) { // On MDN, but not in https://w3c.github.io/FileAPI/#dfn-Blob
              reject(new Error('The File is closed'));
              return;
          }
          */
          var reader = new FileReader();
          reader.addEventListener('load', function () {
            resolve({
              type: f.type,
              stringContents: arraybuffer2string(/** @type {ArrayBuffer} */reader.result),
              name: f.name,
              lastModified: f.lastModified
            });
          });
          // Seems not feasible to accurately simulate
          /* c8 ignore next 3 */
          reader.addEventListener('error', function () {
            reject(reader.error);
          });
          // eslint-disable-next-line @stylistic/max-len -- Long
          // eslint-disable-next-line unicorn/prefer-blob-reading-methods -- Too new?
          reader.readAsArrayBuffer(f);
        });
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var filelist = {
    file: file.file,
    filelist: {
      test: function test(x) {
        return toStringTag(x) === 'FileList';
      },
      replace: function replace(fl) {
        var arr = [];
        for (var i = 0; i < fl.length; i++) {
          arr[i] = fl.item(i);
        }
        return arr;
      },
      revive: function revive(o) {
        /**
         * `FileList` polyfill.
         */
        var FileList = /*#__PURE__*/function () {
          /**
           * Set private properties and length.
           */
          function FileList() {
            _classCallCheck$1(this, FileList);
            // eslint-disable-next-line prefer-rest-params -- API
            this._files = arguments[0];
            this.length = this._files.length;
          }
          /**
           * @param {import('typeson').Integer} index
           * @returns {File}
           */
          return _createClass$1(FileList, [{
            key: "item",
            value: function item(index) {
              return this._files[index];
            }
            /* eslint-disable class-methods-use-this -- Not needed */
            /**
             * @returns {"FileList"}
             */
          }, {
            key: Symbol.toStringTag,
            get: function get() {
              /* eslint-enable class-methods-use-this -- Not needed */
              return 'FileList';
            }
          }]);
        }();
        return new FileList(o);
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var imagebitmap = {
    imagebitmap: {
      test: function test(x) {
        return toStringTag(x) === 'ImageBitmap' ||
        // In Node, our polyfill sets the dataset on a canvas
        //  element as JSDom no longer allows overriding toStringTag
        x && x.dataset && x.dataset.toStringTag === 'ImageBitmap';
      },
      replace: function replace(bm) {
        var canvas = document.createElement('canvas');
        var ctx = /** @type {CanvasRenderingContext2D} */
        canvas.getContext('2d');
        ctx.drawImage(bm, 0, 0);
        return {
          width: bm.width,
          height: bm.height,
          dataURL: canvas.toDataURL()
        };
      },
      revive: function revive(o) {
        var canvas = typeof OffscreenCanvas === 'undefined' ? document.createElement('canvas')
        /* c8 ignore next -- Browser only */ : new OffscreenCanvas(o.width, o.height);
        /*
        var req = new XMLHttpRequest();
        req.open('GET', o, false); // Sync
        if (req.status !== 200 && req.status !== 0) {
          throw new Error('Bad ImageBitmap access: ' + req.status);
        }
        req.send();
        return req.responseText;
        */
        var ctx = /** @type {CanvasRenderingContext2D} */
        canvas.getContext('2d');
        var img = document.createElement('img');
        // The onload is needed by some browsers per http://stackoverflow.com/a/4776378/271577
        img.addEventListener('load', function () {
          ctx.drawImage(img, 0, 0);
        });
        img.src = o.dataURL;
        // Works in contexts allowing an `ImageBitmap` (We might use
        //   `OffscreenCanvas.transferToBitmap` when supported)
        return typeof OffscreenCanvas === 'undefined' ? canvas
        /* c8 ignore next 3 -- Browser only */ : /** @type {OffscreenCanvas} */canvas.transferToImageBitmap();
      },
      reviveAsync: function reviveAsync(o) {
        var canvas = document.createElement('canvas');
        var ctx = /** @type {CanvasRenderingContext2D} */
        canvas.getContext('2d');
        var img = document.createElement('img');
        // The onload is needed by some browsers per http://stackoverflow.com/a/4776378/271577
        img.addEventListener('load', function () {
          ctx.drawImage(img, 0, 0);
        });
        img.src = o.dataURL;
        return new e(/*#__PURE__*/function () {
          var _ref = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(resolve, reject) {
            var resp, _t;
            return _regenerator().w(function (_context) {
              while (1) switch (_context.p = _context.n) {
                case 0:
                  _context.p = 0;
                  _context.n = 1;
                  return createImageBitmap(canvas);
                case 1:
                  resp = _context.v;
                  resolve(resp);
                  /* c8 ignore next 3 */
                  _context.n = 3;
                  break;
                case 2:
                  _context.p = 2;
                  _t = _context.v;
                  reject(_t);
                case 3:
                  return _context.a(2);
              }
            }, _callee, null, [[0, 2]]);
          }));
          return function (_x, _x2) {
            return _ref.apply(this, arguments);
          };
        }());
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var imagedata = {
    imagedata: {
      test: function test(x) {
        return toStringTag(x) === 'ImageData';
      },
      replace: function replace(d) {
        return {
          // Ensure `length` gets preserved for revival
          array: _toConsumableArray$1(d.data),
          width: d.width,
          height: d.height
        };
      },
      revive: function revive(o) {
        return new ImageData(new Uint8ClampedArray(o.array), o.width, o.height);
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var infinity = {
    infinity: {
      test: function test(x) {
        return x === Number.POSITIVE_INFINITY;
      },
      replace: function replace() {
        return 'Infinity';
      } /* n */,
      revive: function revive() {
        return Number.POSITIVE_INFINITY;
      } /* s */
    }
  };

  /**
   * @type {import('typeson').Spec}
   */
  var IntlCollator = {
    test: function test(x) {
      return hasConstructorOf(x, Intl.Collator);
    },
    replace: function replace(c) {
      return c.resolvedOptions();
    },
    revive: function revive(options) {
      return new Intl.Collator(options.locale, options);
    }
  };

  /**
   * @type {import('typeson').Spec}
   */
  var IntlDateTimeFormat = {
    test: function test(x) {
      return hasConstructorOf(x, Intl.DateTimeFormat);
    },
    replace: function replace(dtf) {
      return dtf.resolvedOptions();
    },
    revive: function revive(options) {
      return new Intl.DateTimeFormat(options.locale, options);
    }
  };

  /**
   * @type {import('typeson').Spec}
   */
  var IntlNumberFormat = {
    test: function test(x) {
      return hasConstructorOf(x, Intl.NumberFormat);
    },
    replace: function replace(nf) {
      return nf.resolvedOptions();
    },
    revive: function revive(options) {
      return new Intl.NumberFormat(options.locale, options);
    }
  };
  var intlTypes = {
    IntlCollator: IntlCollator,
    IntlDateTimeFormat: IntlDateTimeFormat,
    IntlNumberFormat: IntlNumberFormat
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var map = {
    map: {
      test: function test(x) {
        return toStringTag(x) === 'Map';
      },
      replace: function replace(mp) {
        return _toConsumableArray$1(mp.entries());
      },
      revive: function revive(entries) {
        return new Map(entries);
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var nan = {
    nan: {
      test: function test(x) {
        return Number.isNaN(x);
      },
      replace: function replace() {
        return 'NaN';
      } /* n */,
      revive: function revive() {
        return Number.NaN;
      } /* s */
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var negativeInfinity = {
    negativeInfinity: {
      test: function test(x) {
        return x === Number.NEGATIVE_INFINITY;
      },
      replace: function replace() {
        return '-Infinity';
      } /* n */,
      revive: function revive() {
        return Number.NEGATIVE_INFINITY;
      } /* s */
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var negativeZero = {
    negativeZero: {
      test: function test(x) {
        return Object.is(x, -0);
      },
      replace: function replace() {
        // Just adding 0 here for minimized space; will still revive as -0
        return 0;
      } /* n */,
      revive: function revive() {
        return -0;
      } /* s */
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var nonbuiltinIgnore = {
    nonbuiltinIgnore: {
      test: function test(x) {
        return x && _typeof$1(x) === 'object' && !Array.isArray(x) && !['Object',
        // `Proxy` and `Reflect`, two other built-in objects, will also
        //   have a `toStringTag` of `Object`; we don't want built-in
        //   function objects, however
        'Boolean', 'Number', 'String', 'Error', 'RegExp', 'Math', 'Date', 'Map', 'Set', 'JSON', 'ArrayBuffer', 'SharedArrayBuffer', 'DataView', 'Int8Array', 'Uint8Array', 'Uint8ClampedArray', 'Int16Array', 'Uint16Array', 'Int32Array', 'Uint32Array', 'Float32Array', 'Float64Array', 'Promise', 'String Iterator', 'Array Iterator', 'Map Iterator', 'Set Iterator', 'WeakMap', 'WeakSet', 'Atomics', 'Module'].includes(toStringTag(x));
      },
      replace: function replace() {
        // Not in use
      } /* rexp */
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var primitiveObjects = {
    // String Object (not primitive string which need no type spec)
    StringObject: {
      test: function test(x) {
        return toStringTag(x) === 'String' && _typeof$1(x) === 'object';
      },
      replace: String,
      // convert to primitive string
      revive: function revive(s) {
        return new String(s);
      } // Revive to an objectified string
    },
    // Boolean Object (not primitive boolean which need no type spec)
    BooleanObject: {
      test: function test(x) {
        return toStringTag(x) === 'Boolean' && _typeof$1(x) === 'object';
      },
      replace: function replace(o) {
        // convert to primitive boolean
        return o.valueOf();
      },
      revive: function revive(b) {
        // Revive to an objectified Boolean
        return new Boolean(b);
      }
    },
    // Number Object (not primitive number which need no type spec)
    NumberObject: {
      test: function test(x) {
        return toStringTag(x) === 'Number' && _typeof$1(x) === 'object';
      },
      replace: Number,
      // convert to primitive number
      revive: function revive(n) {
        return new Number(n);
      } // Revive to an objectified number
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var promise = {
    promise: {
      test: function test(x) {
        return toStringTag(x) === 'Promise';
      },
      replaceAsync: function replaceAsync(prom) {
        return new e(/*#__PURE__*/function () {
          var _ref = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(resolve) {
            var _t, _t2, _t3;
            return _regenerator().w(function (_context) {
              while (1) switch (_context.p = _context.n) {
                case 0:
                  _context.p = 0;
                  _t = resolve;
                  _context.n = 1;
                  return prom;
                case 1:
                  _t2 = _context.v;
                  _t({
                    value: _t2
                  });
                  _context.n = 3;
                  break;
                case 2:
                  _context.p = 2;
                  _t3 = _context.v;
                  resolve({
                    error: _t3
                  });
                case 3:
                  return _context.a(2);
              }
            }, _callee, null, [[0, 2]]);
          }));
          return function (_x) {
            return _ref.apply(this, arguments);
          };
        }());
      },
      revive: function revive(o) {
        return o.error ? Promise.reject(o.error) : Promise.resolve(o.value);
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var regexp = {
    regexp: {
      test: function test(x) {
        return toStringTag(x) === 'RegExp';
      },
      replace: function replace(rexp) {
        return {
          source: rexp.source,
          flags: (rexp.global ? 'g' : '') + (rexp.ignoreCase ? 'i' : '') + (rexp.multiline ? 'm' : '') + (rexp.sticky ? 'y' : '') + (rexp.unicode ? 'u' : '') + (rexp.unicodeSets ? 'v' : '') + (rexp.hasIndices ? 'd' : '') + (rexp.dotAll ? 's' : '')
        };
      },
      revive: function revive(_ref) {
        var source = _ref.source,
          flags = _ref.flags;
        return new RegExp(source, flags);
      }
    }
  };

  /**
   * @type {{[key: string]: any}}
   */
  var resurrectableObjectsByUUID = {};

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var resurrectable = {
    resurrectable: {
      test: function test(x) {
        return x && !Array.isArray(x) && ['object', 'function', 'symbol'].includes(_typeof$1(x));
      },
      replace: function replace(rsrrctble) {
        var uuid = generateUUID();
        resurrectableObjectsByUUID[uuid] = rsrrctble;
        return uuid;
      },
      revive: function revive(serializedResurrectable) {
        return resurrectableObjectsByUUID[serializedResurrectable];
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var set = {
    set: {
      test: function test(x) {
        return toStringTag(x) === 'Set';
      },
      replace: function replace(st) {
        return _toConsumableArray$1(st.values());
      },
      revive: function revive(values) {
        return new Set(values);
      }
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var symbol = {
    symbol: {
      test: function test(x) {
        return _typeof$1(x) === 'symbol';
      },
      replace: function replace(sym) {
        return {
          global: Symbol.keyFor(sym) !== undefined,
          sym: String(sym).slice(7, -1)
        };
      },
      revive: function revive(o) {
        return o.global ? Symbol["for"](o.sym) : Symbol(o.sym);
      }
    }
  };

  // Support all kinds of typed arrays (views of ArrayBuffers)

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var typedArraysSocketIO = {};

  /**
   * @param {Int8ArrayConstructor|Uint8ArrayConstructor|
   *   Uint8ClampedArrayConstructor|Int16ArrayConstructor|
   *   Uint16ArrayConstructor|Int32ArrayConstructor|
   *   Uint32ArrayConstructor|Float32ArrayConstructor|
   *   Float64ArrayConstructor|
   *   BigInt64ArrayConstructor|BigUint64ArrayConstructor
   * } TypedArray
   * @returns {void}
   */
  function create$1(TypedArray) {
    var typeName = TypedArray.name;
    typedArraysSocketIO[typeName.toLowerCase()] = {
      test: function test(x) {
        return toStringTag(x) === typeName;
      },
      replace: function replace(a) {
        return (a.byteOffset === 0 && a.byteLength === a.buffer.byteLength ? a
        // socket.io supports streaming ArrayBuffers.
        // If we have a typed array representing a portion
        //   of the buffer, we need to clone
        //   the buffer before leaving it to socket.io.
        : a.slice(0)).buffer;
      },
      revive: function revive(buf) {
        // One may configure socket.io to revive binary data as
        //    Buffer or Blob.
        // We should therefore not rely on that the instance we
        //   get here is an ArrayBuffer
        // If not, let's assume user wants to receive it as
        //   configured with socket.io.
        return toStringTag(buf) === 'ArrayBuffer' ? new TypedArray(buf) : buf;
      }
    };
  }
  if (typeof Int8Array === 'function') {
    // Those constructors are added in ES6 as a group.
    // If we have Int8Array, we can assume the rest also exists.

    [Int8Array, Uint8Array, Uint8ClampedArray, Int16Array, Uint16Array, Int32Array, Uint32Array, Float32Array, Float64Array].concat(_toConsumableArray$1(typeof BigInt64Array === 'function' ? [BigInt64Array, BigUint64Array]
    /* c8 ignore next */ : [])).forEach(function (TypedArray) {
      return create$1(TypedArray);
    });
  }

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var typedArrays = {};

  /**
   * @typedef {Int8ArrayConstructor|Uint8ArrayConstructor|
   *   Uint8ClampedArrayConstructor|
   *   Int16ArrayConstructor|Uint16ArrayConstructor|
   *   Int32ArrayConstructor|Uint32ArrayConstructor|
   *   Float32ArrayConstructor|
   *   Float64ArrayConstructor|
   *   BigInt64ArrayConstructor|BigUint64ArrayConstructor} TypedArrayConstructor
   */

  /**
   * @param {TypedArrayConstructor} TypedArray
   * @returns {void}
   */
  function create(TypedArray) {
    var typeName = TypedArray.name;
    typedArrays[typeName.toLowerCase()] = {
      test: function test(x) {
        return toStringTag(x) === typeName;
      },
      replace: function replace(_ref,
      /**
       * @type {import('typeson').StateObject & {
       *   buffers?: ArrayBuffer[]
       * }}
       */
      stateObj) {
        var buffer = _ref.buffer,
          byteOffset = _ref.byteOffset,
          l = _ref.length;
        if (!stateObj.buffers) {
          stateObj.buffers = [];
        }
        var index = stateObj.buffers.indexOf(buffer);
        if (index !== -1) {
          return {
            index: index,
            byteOffset: byteOffset,
            length: l
          };
        }
        stateObj.buffers.push(buffer);
        return {
          maxByteLength: buffer.maxByteLength,
          resizable: buffer.resizable,
          encoded: encode(buffer),
          byteOffset: byteOffset,
          length: l
        };
      },
      revive: function revive(b64Obj,
      /**
       * @type {import('typeson').StateObject & {
       *   buffers?: ArrayBuffer[]
       * }}
       */
      stateObj) {
        if (!stateObj.buffers) {
          stateObj.buffers = [];
        }
        var byteOffset = b64Obj.byteOffset,
          len = b64Obj.length,
          encoded = b64Obj.encoded,
          index = b64Obj.index,
          maxByteLength = b64Obj.maxByteLength,
          resizable = b64Obj.resizable;
        var buffer;
        if ('index' in b64Obj) {
          buffer = stateObj.buffers[index];
        } else {
          buffer = decode(encoded, resizable ? {
            maxByteLength: maxByteLength
          } : undefined);
          stateObj.buffers.push(buffer);
        }
        return new TypedArray(buffer, byteOffset, len);
      }
    };
  }
  if (typeof Int8Array === 'function') {
    // Those constructors are added in ES6 as a group.
    // If we have Int8Array, we can assume the rest also exists.
    [Int8Array, Uint8Array, Uint8ClampedArray, Int16Array, Uint16Array, Int32Array, Uint32Array, Float32Array, Float64Array].concat(_toConsumableArray$1(typeof BigInt64Array === 'function' ? [BigInt64Array, BigUint64Array]
    /* c8 ignore next */ : [])).forEach(function (TypedArray) {
      return create(TypedArray);
    });
  }

  // This does not preserve `undefined` in sparse arrays; see the `undef`
  //  or `sparse-undefined` preset

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var undef$1 = {
    undef: {
      test: function test(x, stateObj) {
        return typeof x === 'undefined' && (stateObj.ownKeys || !('ownKeys' in stateObj));
      },
      replace: function replace() {
        return 0;
      } /* n */,
      revive: function revive() {
        // Will add `undefined` (returning `undefined` would instead
        //   avoid explicitly setting)
        return new s();
      } /* s */
    }
  };

  /**
   * @type {import('typeson').TypeSpecSet}
   */
  var userObject = {
    userObject: {
      test: function test(x /* , stateObj */) {
        return isUserObject(x);
      },
      replace: function replace(n) {
        return _objectSpread2$1({}, n);
      },
      revive: function revive(s) {
        return s;
      }
    }
  };

  /**
   * @type {import('typeson').Preset}
   */
  var arrayNonindexKeys = [{
    arrayNonindexKeys: {
      testPlainObjects: true,
      test: function test(x, stateObj) {
        if (Array.isArray(x)) {
          if (
          // By avoiding serializing arrays into objects which
          //  have only positive-integer keys, we reduce
          //  size and improve revival performance; arrays with
          //  non-index keys will be larger however
          Object.keys(x).some(function (k) {
            //  No need to check for `isNaN` or
            //   `isNaN(Number.parseInt())` as `NaN` will be
            //   treated as a string.
            //  No need to do check as
            //   `Number.parseInt(Number())` since scientific
            //   notation will be pre-resolved if a number
            //   was given, and it will otherwise be a string
            return String(Number.parseInt(k)) !== k;
          })) {
            stateObj.iterateIn = 'object';
            stateObj.addLength = true;
          }
          return true;
        }
        return false;
      },
      replace: function replace(a, stateObj) {
        // Catch sparse undefined
        stateObj.iterateUnsetNumeric = true;
        return a;
      },
      revive: function revive(o) {
        if (Array.isArray(o)) {
          return o;
        }

        /**
         * @type {{[key: string]: any}}
         */
        var arr = [];
        // No map here as may be a sparse array (including
        //   with `length` set)
        Object.entries(o).forEach(function (_ref) {
          var _ref2 = _slicedToArray$1(_ref, 2),
            key = _ref2[0],
            val = _ref2[1];
          arr[key] = val;
        });
        return arr;
      }
    }
  }, {
    sparseUndefined: {
      test: function test(x, stateObj) {
        return typeof x === 'undefined' && stateObj.ownKeys === false;
      },
      replace: function replace() {
        return 0;
      } /* n */,
      revive: function revive() {
        return undefined;
      } /* s */ // Will avoid adding anything
    }
  }];

  /**
   * @type {import('typeson').Preset}
   */
  var specialNumbers = [nan, infinity, negativeInfinity, negativeZero];

  /* This preset includes types that are built-in into the JavaScript
      language itself, this should work universally.

    Types that were added in ES6 or beyond will be checked before inclusion
     so that this module can be consumed by both ES5 and ES6 environments.

    Some types cannot be encapsulated because their inner state is private:
      `WeakMap`, `WeakSet`.

    The Function type is not included because their closures would not be
      serialized, so a revived Function that uses closures would not behave
      as expected.

    Symbols are similarly not included.
  */


  /**
   * @type {import('typeson').Preset}
   */
  var expObj$1 = [undef$1,
  // ES5
  arrayNonindexKeys, primitiveObjects, specialNumbers, date, error, errors, regexp].concat(
  // ES2015 (ES6)
  /* c8 ignore next */
  typeof Map === 'function' ? map : [], /* c8 ignore next */
  typeof Set === 'function' ? set : [], /* c8 ignore next */
  typeof ArrayBuffer === 'function' ? arraybuffer : [], /* c8 ignore next */
  typeof Uint8Array === 'function' ? typedArrays : [], /* c8 ignore next */
  typeof DataView === 'function' ? dataview : [], /* c8 ignore next */
  typeof Intl !== 'undefined' ? intlTypes : [], /* c8 ignore next */
  typeof BigInt !== 'undefined' ? [bigint, bigintObject] : []);

  /*
  When communicating via `postMessage()` (`Worker.postMessage()` or
  `window.postMessage()`), the browser will use a similar algorithm as Typeson
  does to encapsulate and revive all items in the structure (aka the structured
  clone algorithm). This algorithm supports all built-in types as well as many
  DOM types. Therefore, only types that are not included in the structured clone
  algorithm need to be registered, which is:

  * Error
  * Specific Errors like SyntaxError, TypeError, etc.
  * Any custom type you want to send across window- or worker boundraries

  This preset will only include the Error types and you can register your
  custom types after having registered these.
  */


  /**
   * @type {import('typeson').Preset}
   */
  var postmessage = [error, errors];

  /**
   * @type {import('typeson').Preset}
   */
  var socketio = [expObj$1,
  // Leave ArrayBuffer as is, and let socket.io stream it instead.
  {
    arraybuffer: null
  },
  // Encapsulate TypedArrays in ArrayBuffers instead of base64 strings.
  typedArraysSocketIO];

  /**
   * @type {import('typeson').Preset}
   */
  var sparseUndefined = [{
    sparseArrays: {
      testPlainObjects: true,
      test: function test(x) {
        return Array.isArray(x);
      },
      replace: function replace(a, stateObj) {
        stateObj.iterateUnsetNumeric = true;
        return a;
      }
    }
  }, {
    sparseUndefined: {
      test: function test(x, stateObj) {
        return typeof x === 'undefined' && stateObj.ownKeys === false;
      },
      replace: function replace() {
        return 0;
      } /* n */,
      revive: function revive() {
        return undefined;
      } /* s */ // Will avoid adding anything
    }
  }];

  /* This preset includes types for the Structured Cloning Algorithm. */


  /**
   * @type {import('typeson').Preset}
   */
  var expObj = [
  // ES5
  userObject,
  // Processed last (non-builtin)

  undef$1, arrayNonindexKeys, primitiveObjects, specialNumbers, date, regexp,
  // Non-built-ins
  imagedata, imagebitmap,
  // Async return
  file, filelist, blob, error, errors].concat(
  // ES2015 (ES6)
  /* c8 ignore next */
  typeof Map === 'function' ? map : [], /* c8 ignore next */
  typeof Set === 'function' ? set : [], /* c8 ignore next */
  typeof ArrayBuffer === 'function' ? arraybuffer : [], /* c8 ignore next */
  typeof Uint8Array === 'function' ? typedArrays : [], /* c8 ignore next */
  typeof DataView === 'function' ? dataview : [], /* c8 ignore next */
  typeof crypto !== 'undefined' ? cryptokey : [], /* c8 ignore next */
  typeof BigInt !== 'undefined' ? [bigint, bigintObject] : [], /* c8 ignore next */
  typeof DOMException !== 'undefined' ? domexception : [], /* c8 ignore next */
  typeof DOMRect !== 'undefined' ? domrect : [], /* c8 ignore next */
  typeof DOMPoint !== 'undefined' ? dompoint : [], /* c8 ignore next */
  typeof DOMQuad !== 'undefined' ? domquad : [], /* c8 ignore next */
  typeof DOMMatrix !== 'undefined' ? dommatrix : []);

  /**
   * @type {import('typeson').Preset}
   */
  var structuredCloningThrowing = expObj.concat({
    checkDataCloneException: {
      test: function test(val) {
        // Should also throw with:
        // 1. `IsDetachedBuffer` (a process not called within the
        //      ECMAScript spec)
        // 2. `IsCallable` (covered by `typeof === 'function'` or a
        //       function's `toStringTag`)
        // 3. internal slots besides [[Prototype]] or [[Extensible]] (e.g.,
        //        [[PromiseState]] or [[WeakMapData]])
        // 4. exotic object (e.g., `Proxy`) (unless an `%ObjectPrototype%`
        //      intrinsic object) (which does not have default
        //      behavior for one or more of the essential internal methods
        //      that are limited to the following for non-function objects
        //      (we auto-exclude functions):
        //      [[GetPrototypeOf]],[[SetPrototypeOf]],[[IsExtensible]],
        //      [[PreventExtensions]],[[GetOwnProperty]],
        //      [[DefineOwnProperty]],[[HasProperty]],
        //      [[Get]],[[Set]],[[Delete]],[[OwnPropertyKeys]]);
        //      except for the standard, built-in exotic objects, we'd need
        //      to know whether these methods had distinct behaviors
        // Note: There is no apparent way for us to detect a `Proxy` and
        //      reject (Chrome at least is not rejecting anyways)
        var stringTag = {}.toString.call(val).slice(8, -1);
        if ([
        // Symbol's `toStringTag` is only "Symbol" for its initial
        //   value, so we check `typeof`
        'symbol',
        // All functions including bound function exotic objects
        'function'].includes(_typeof$1(val)) || [
        // A non-array exotic object
        'Arguments',
        // A non-array exotic object
        'Module',
        // Promise instances have an extra slot ([[PromiseState]])
        //    but not throwing in Chrome `postMessage`
        'Promise',
        // WeakMap instances have an extra slot ([[WeakMapData]])
        //    but not throwing in Chrome `postMessage`
        'WeakMap',
        // WeakSet instances have an extra slot ([[WeakSetData]])
        //    but not throwing in Chrome `postMessage`
        'WeakSet',
        // HTML-SPECIFIC
        'Event',
        // Also in Node `worker_threads` (currently experimental)
        'MessageChannel'].includes(stringTag) || (
        /*
        // isClosed is no longer documented
        ((stringTag === 'Blob' || stringTag === 'File') &&
            val.isClosed) ||
        */
        val && _typeof$1(val) === 'object' &&
        // Duck-type DOM node objects (non-array exotic?
        //    objects which cannot be cloned by the SCA)
        typeof val.nodeType === 'number' && typeof val.insertBefore === 'function')) {
          throw new DOMException('The object cannot be cloned.', 'DataCloneError');
        }
        return false;
      }
    }
  });

  /**
   * @type {import('typeson').Preset}
   */
  var undef = [sparseUndefined, undef$1];

  /**
   * @type {import('typeson').Preset}
   */
  var universal = [expObj$1
  // TODO: Add types that are de-facto universal even though not
  //   built-in into ecmasript standard.
  ];

  exports.JSON_TYPES = u;
  exports.Typeson = c;
  exports.TypesonPromise = e;
  exports.Undefined = s;
  exports.arrayNonindexKeys = arrayNonindexKeys;
  exports.arraybuffer = arraybuffer;
  exports.bigint = bigint;
  exports.bigintObject = bigintObject;
  exports.blob = blob;
  exports.builtin = expObj$1;
  exports.cloneable = cloneable;
  exports.cryptokey = cryptokey;
  exports.dataview = dataview;
  exports.date = date;
  exports.domexception = domexception;
  exports.dommatrix = dommatrix;
  exports.dompoint = dompoint;
  exports.domquad = domquad;
  exports.domrect = domrect;
  exports.error = error;
  exports.errors = errors;
  exports.escapeKeyPathComponent = escapeKeyPathComponent;
  exports.file = file;
  exports.filelist = filelist;
  exports.getByKeyPath = getByKeyPath;
  exports.getJSONType = getJSONType;
  exports.hasConstructorOf = hasConstructorOf;
  exports.imagebitmap = imagebitmap;
  exports.imagedata = imagedata;
  exports.infinity = infinity;
  exports.intlTypes = intlTypes;
  exports.isObject = isObject;
  exports.isPlainObject = isPlainObject;
  exports.isThenable = isThenable;
  exports.isUserObject = isUserObject;
  exports.map = map;
  exports.nan = nan;
  exports.negativeInfinity = negativeInfinity;
  exports.negativeZero = negativeZero;
  exports.nonbuiltinIgnore = nonbuiltinIgnore;
  exports.postmessage = postmessage;
  exports.primitiveObjects = primitiveObjects;
  exports.promise = promise;
  exports.regexp = regexp;
  exports.resurrectable = resurrectable;
  exports.set = set;
  exports.setAtKeyPath = setAtKeyPath;
  exports.socketio = socketio;
  exports.sparseUndefined = sparseUndefined;
  exports.specialNumbers = specialNumbers;
  exports.structuredCloning = expObj;
  exports.structuredCloningThrowing = structuredCloningThrowing;
  exports.symbol = symbol;
  exports.toStringTag = toStringTag;
  exports.typedArrays = typedArrays;
  exports.typedArraysSocketio = typedArraysSocketIO;
  exports.undef = undef$1;
  exports.undefPreset = undef;
  exports.unescapeKeyPathComponent = unescapeKeyPathComponent;
  exports.universal = universal;
  exports.userObject = userObject;

}));
//# sourceMappingURL=index.umd.cjs.map
